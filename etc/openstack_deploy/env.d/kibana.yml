---
component_skel:
  kibana:
    belongs_to:
      - kibana_all

container_skel:
  kibana_container:
    belongs_to:
      - log_containers
    contains:
      - kibana
    properties:
      service_name: kibana
      container_release: trusty

type              : agent.memory
label             : memory_check--{{ inventory_hostname|quote }}
disabled          : false
period            : "{{ maas_check_period }}"
timeout           : "{{ maas_check_timeout }}"
alarms            :
    memory_used                 :
        label                   : memory_check--{{ inventory_hostname|quote }}
        notification-plan       : "{{ maas_notification_plan }}"
        criteria                : |
            consecutiveCount={{ maas_alarm_local_consecutive_count }}
            if (percentage(metric["actual_used"], metric["total"]) >= 95.0) {
                return new AlarmStatus(WARNING, "Memory is 95%+ in use.");
            }
